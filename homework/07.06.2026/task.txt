 Tonight's homework: in a new homework/2026.07.06/ folder (same pattern as before — scratch files, not the real site), build a
  small practice page that exercises what you just learned:
  - A handful of clickable elements (your choice of content/theme).
  - Use querySelectorAll + forEach to attach a click listener to all of them at once, rather than one at a time.
  - Deliberately structure your HTML so that reaching the right elements requires thinking about > (direct child) vs a plain
  descendant selector — set up at least one case where a > selector would fail structurally, the way .interest > li did tonight, so
  you have to reason through it again fresh.

  No specific visual effect required for the click — console.log, a style change, a prompt, whatever you want.

  Also still outstanding from a couple sessions back, worth a quick pass if you have time: explain out loud (or in a comment to
  yourself) why flex-grow/flex-shrink/flex-basis behave the way they do, and why your CSS custom properties live on :root rather
  than body. That one's pure recall, no new code needed.
